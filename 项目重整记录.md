每次实验必须记录：
- git commit
- 数据 YAML
- class names / prompt 顺序
- model weights
- task 类型
- 是否保留 YOLOESegModel
- 是否关闭 mask loss
- train/val/test 数据
- freeze 层数
- 是否 KD
- 是否 DINO
- AP50 / P / R / FP / FN
- ship AP
- bridge AP
########################
11/06/2026
目前在这个分支：4019dec42c406462582d291dce44f52ae980d5cf
然后数据YAML还没做。
class names暂定还是两个怎么样，一个ship一个bridge
model weights没拆分呢
task 类型暂定seg
其他的先不管了
**所以今天的任务是先分数据**
数据集位置：C:\Users\DOCTOR\Documents\GitHub\YOLOv8\ultralytics\datasets\bridgeAll
labels确认是没问题的
ship   -> 0
bridge -> 3
13/06/2026
此模型拆分结论：
best.pt inspection result:

- checkpoint loads from local clean Ultralytics successfully.
- model class: YOLOESegModel
- task: segment
- last layer: YOLOESegment
- original names: {0: ship, 1: harbor, 2: tank}
- total params: 11.791M
- layers: 23 total
- rough structure:
  - 0-9 backbone
  - 10-21 neck
  - 22 YOLOESegment head
- YOLOE prompt functions exist:
  - get_text_pe: yes
  - set_classes: yes
- get_text_pe(["ship","harbor","tank","bridge"]) succeeds.
- returned text embedding shape: [1, 4, 512]
- direct set_classes(names) fails because this branch requires set_classes(names, embeddings).
- next required check:
  embeddings = model.get_text_pe(global4_names)
  model.set_classes(global4_names, embeddings)
  then verify names and head nc/pe state.
######################################
# Branch Record: bridge-init + ship-pseudo 2-class student baseline

## 1. 实验目的

本分支尝试构建一个普通 YOLOv8m 二类 student：

```text
0 ship
1 bridge
```

核心思路是：

```text
bridge 能力：来自 bridge-only YOLOv8m expert 初始化
ship 能力：来自 YOLOE best.pt 生成的离线 hard pseudo labels
bridge 监督：来自 bridge GT，原 class 0 remap 到 class 1
```

该实验不是 late fusion，也不是 YOLOE 权重/向量迁移。YOLOE `ultralytics/best.pt` 没有作为 student 初始化，只作为冻结 ship teacher 生成 pseudo label。

---

## 2. 旧数据问题

旧数据集：

```text
ultralytics/datasets/bridge_ship_distill_2cls
```

存在关键问题：

```text
train:
  ship class 0:   477 boxes / 240 images
  bridge class 1: 88 boxes / 72 images

val:
  ship class 0:   0
  bridge class 1: 25 boxes / 18 images
```

因此旧训练日志中 native validation 只显示：

```text
all
bridge
```

没有显示 ship。
这说明旧数据集不能用于判断二类 student 是否学回 ship。

---

## 3. v2 数据集修复

新建数据集：

```text
ultralytics/datasets/bridge_ship_distill_2cls_v2
```

类别定义：

```text
0 ship
1 bridge
```

训练 YAML：

```text
ultralytics/datasets/bridge_ship_distill_2cls_v2/data.yaml
```

数据分布：

```text
train:
  images: 312
  ship class 0:   513 boxes
  bridge class 1: 99 boxes

val:
  images: 78
  ship class 0:   114 boxes
  bridge class 1: 19 boxes

test_ship:
  images: 300
  ship class 0: 694 boxes

test_bridge:
  images: 532
  bridge class 1: 798 boxes
```

数据来源：

```text
ship train:
  图像来自 ship_small_split/images/train
  标签来自 YOLOE best.pt 生成的 ship pseudo labels
  class = 0

ship val:
  图像来自 ship_small_split/images/val
  标签使用原始 GT
  class = 0

ship test:
  来自 ship_extratest_no_overlap
  标签使用 GT
  class = 0

bridge train:
  来自 total_bridge_trainval/train_rgb、train_sar、train_ir
  每模态抽取 24 张，共 72 张
  原 bridge class 0 remap 为 class 1

bridge val:
  来自 total_bridge_trainval/val_rgb、val_sar、val_ir
  每模态抽取 6 张，共 18 张
  原 bridge class 0 remap 为 class 1

bridge test:
  来自完整 total_bridge_test
  原 bridge class 0 remap 为 class 1
```

确认事项：

```text
train/val 都同时包含 ship 和 bridge。
bridge 标签已完成 0 -> 1 remap。
ship 标签保持 class 0。
test_ship 和 test_bridge 未写入训练 data.yaml。
total_bridge_test 未进入 train/val。
ship_extratest_no_overlap 未进入 train/val。
missing labels = 0。
bad rows = 0。
```

备注：

```text
ship train 使用 YOLOE best.pt 重新生成 pseudo labels。
conf=0.25，imgsz=1024。
有 1 张 ship train 图像 sar_0009388.jpg 生成 empty pseudo，已记录。
```

---

## 4. 训练与评估结果

使用 v2 数据集训练普通 YOLOv8m 二类 student。

student 初始化：

```text
bridge-only YOLOv8m expert:
  old class 0 bridge -> student class 1 bridge
```

评估结果：

### test_ship

```text
Images: 300
Instances: 694

Precision: 0.659
Recall:    0.356
mAP50:     0.390
mAP50-95:  0.196
```

### test_bridge

```text
Images: 532
Instances: 798

Precision: 0.841
Recall:    0.625
mAP50:     0.726
mAP50-95:  0.425
```

### test_combined

```text
Images: 832
Instances: 1492

all:
  Precision: 0.704
  Recall:    0.490
  mAP50:     0.538
  mAP50-95:  0.301

ship:
  Precision: 0.567
  Recall:    0.360
  mAP50:     0.357
  mAP50-95:  0.181

bridge:
  Precision: 0.840
  Recall:    0.620
  mAP50:     0.719
  mAP50-95:  0.420
```

---

## 5. 结论

该实验验证了以下几点：

1. v2 数据集修复是成功的。
   旧数据集 val 缺少 ship 的问题已经解决，新的 train/val 都包含 ship 和 bridge。

2. bridge 初始化有效，但不是最终上界。
   bridge 在二类 student 中达到 mAP50≈0.726，说明 bridge-only expert 初始化确实带来了 bridge 能力。但该结果仍低于 bridge-only expert 的独立上界。

3. ship pseudo hard-label 迁移失败。
   原始 YOLOE best.pt 的 ship 外部 AP50 约为 0.92，而本二类 student 在 test_ship 上只有 AP50≈0.39。说明少量离线 pseudo labels 没有成功迁移 YOLOE best.pt 的 ship 能力。

4. 单 student 路线当前不值得继续作为主线。
   当前结构只能在普通 YOLOv8m student 中选择 bridge 强初始化，然后用 pseudo label 间接学习 ship。结果显示 ship 能力损失过大，无法满足“ship 保持 + bridge 增强”的目标。

---

## 6. 分支记录结论

本分支应记录为：

```text
bridge-init + ship-pseudo hard-label 2-class student baseline
```

实验结论：

```text
数据构建问题已解决；
bridge 能力部分保留；
ship 能力严重丢失；
离线 hard pseudo label 不能替代 YOLOE best.pt 的 ship 能力；
后续主线应转回 late fusion 上界方案。
```

下一步主线：

```text
ship expert:
  原始 YOLOE best.pt，冻结推理，负责 ship 输出

bridge expert:
  bridge-only YOLOv8m expert，负责 bridge 输出

fusion:
  prediction-level late fusion
  ship 输出映射为 class 0
  bridge 输出映射为 class 1
  合并 boxes 后做 NMS / class-aware 或 class-agnostic 策略比较
```









